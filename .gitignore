# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Dependencies / build (falls einzelne Projekte welche nutzen)
node_modules/
dist/
.cache/

# wrangler / Cloudflare Pages — lokaler Cache (Account-Daten, wird neu erzeugt)
.wrangler/

# Secrets — niemals committen
.env
.env.*
*.key

# geklontes Referenz-Repo (eigenes git)
_referenz/awesome-design-md/
